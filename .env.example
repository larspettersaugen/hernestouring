# PostgreSQL — local Docker example:
#   docker run --name touring-pg -e POSTGRES_PASSWORD=dev -p 5432:5432 -d postgres:16
# Production: Neon, Supabase, or Vercel Postgres (use pooled + sslmode=require when offered).
DATABASE_URL="postgresql://postgres:dev@localhost:5432/postgres"
NEXTAUTH_SECRET="generate-a-random-secret-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Optional: public URL for invite/SMS links (tunnel ngrok, etc.) when you use the app on localhost but
# testers open links on another device. Deployed apps usually need no extra var if Host / NEXTAUTH_URL is correct.
# PUBLIC_APP_URL="https://your-app.example.com"

# Optional: shareable beta signup — set a long random string (e.g. openssl rand -hex 32).
# Share: {NEXTAUTH_URL}/join/{BETA_JOIN_SECRET} — testers create viewer account + Person profile.
# Leave unset to disable public self-signup.
# BETA_JOIN_SECRET=""

# Google OAuth (optional - add to enable "Sign in with Google")
# Create credentials at https://console.cloud.google.com/apis/credentials
# Add http://localhost:3000/api/auth/callback/google to Authorized redirect URIs
# GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-client-secret"

# Email invites via Resend – get API key at https://resend.com/api-keys
RESEND_API_KEY="re_xxxx"
RESEND_FROM_EMAIL="onboarding@resend.dev"

# Flight lookup via aviationstack – get key at https://aviationstack.com/signup/free
AVIATIONSTACK_ACCESS_KEY=""
