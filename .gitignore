node_modules
# Secrets — never git add -f .env / .env.local (real DATABASE_URL, API keys).
.env
uploads
prisma/dev.db
prisma/dev.db-journal

# Next.js — never commit; tracked .next breaks dev (missing CSS / wrong chunks)
.next/
out/
.vercel
.env*.local
tsconfig.tsbuildinfo
